{
  "name": "whmcs-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for WHMCS API integration",
  "type": "module",
  "bin": {
    "whmcs-mcp-server": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "npm run build && node --test test/*.test.mjs",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "whmcs",
    "api",
    "tickets"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "axios": "^1.7.9",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
